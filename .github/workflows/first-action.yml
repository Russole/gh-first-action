name: First Workflow
# event
on: workflow_dispatch
jobs:
  # 自訂義job名稱
  first-job:
    # 執行workflow的環境
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello World!"
      - name: Print goodbye
        run: echo "Done - bye!"

    
